<li>
  <%= link_to portfolio_item.title, admin_page_path(portfolio_item) %>
  <%= link_to "Edit", edit_admin_page_path(portfolio_item) %>
  <%= link_to "Delete", admin_page_path(portfolio_item), method: :delete,
          data: { confirm: "Are you sure you want to delete this Portfolio Item?" } %>
</li>
